<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="20dp">

    <!-- Levý panel: tlačítka pod sebou -->
    <Button
        android:id="@+id/btnLasagne"
        android:layout_width="130dp"
        android:layout_height="wrap_content"
        android:text="Lasagne"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btnSalat"
        android:layout_width="130dp"
        android:layout_height="wrap_content"
        android:text="Salát"
        app:layout_constraintTop_toBottomOf="@id/btnLasagne"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btnVejce"
        android:layout_width="130dp"
        android:layout_height="wrap_content"
        android:text="Vejce"
        app:layout_constraintTop_toBottomOf="@id/btnSalat"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <!-- Spodní panel: fragment container -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/btnVejce"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="20dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="#F4F0F0"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
